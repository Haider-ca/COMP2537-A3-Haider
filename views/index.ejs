<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pokémon Memory Game</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body class="light-theme">

    <div class="container py-4">
        <h1 class="text-center mb-4">Pokémon Memory Game</h1>

        <div class="d-flex flex-wrap justify-content-between align-items-center mb-3">
            <div>
                <label class="me-2">Difficulty:</label>
                <select id="difficulty" class="form-select d-inline-block w-auto">
                    <option value="easy">Easy (6 cards)</option>
                    <option value="medium">Medium (12 cards)</option>
                    <option value="hard">Hard (18 cards)</option>
                </select>
            </div>
            <div>
                <button id="start-btn" class="btn btn-primary me-2">Start</button>
                <button id="reset-btn" class="btn btn-secondary">Reset</button>
            </div>
            <div>
                <label class="me-2">Theme:</label>
                <select id="theme" class="form-select d-inline-block w-auto">
                    <option value="light">Light</option>
                    <option value="dark">Dark</option>
                </select>
            </div>
            <div>
                <button id="powerup-btn" class="btn btn-warning">Power-Up</button>
            </div>
        </div>

        <!-- Status Header -->
        <div id="status" class="mb-3 p-2 bg-light border rounded fw-bold">
            <span>Clicks: <span id="clicks">0</span></span> |
            <span>Matched: <span id="matched">0</span>/<span id="total-pairs">0</span></span> |
            <span>Remaining: <span id="remaining">0</span></span> |
            <span>Time Left: <span id="timer">00:00</span></span>
        </div>

        <div id="game-message"></div>

        <!-- Game Board -->
        <div id="game-board" class="row g-3 justify-content-center"></div>
    </div>

    <!-- Game Logic -->
    <script src="/js/game.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>